from node:<version>
	* 18.5.0;


# Docker 기본 명령어

# Docker build

`docker build -t getting started`;



## 컨테이너 실행
docker run [Option]
docker run -it ....

`docker run -d p 3000:3000 -p 8080:8080 --name front`

## docker ps;


## docker run ;

## docker stop;

## docker rm;

